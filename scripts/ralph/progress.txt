# Ralph Progress Log
Started: Wed Mar 18 04:41:23 AM UTC 2026

## Codebase Patterns
- Prisma schema uses multi-file layout in `packages/db/prisma/schema/` — each domain gets its own `.prisma` file
- IDs use `@default(cuid(2))` across all models
- Table names use `@@map("snake_case")` convention
- Foreign keys get `@@index([fieldName])` declarations
- Relations use `onDelete: Cascade` for user-owned entities
- Project uses `db:push` (no migration files) — validate schema with `prisma generate`
- Pre-existing typecheck error: `web-push` module lacks type declarations (not blocking)
- oRPC routers: plain objects in `packages/api/src/routers/`, registered in `routers/index.ts` appRouter
- Use `protectedProcedure` for auth routes, access userId via `context.session.user.id`
- Turbo task for typechecking is `check-types` (not `typecheck`)
- TanStack Router route tree (`routeTree.gen.ts`) is generated by the vite plugin at build time; to regenerate manually, run `pnpm vite build` from `apps/web/` (the `@tanstack/router-cli generate` command alone may not work); delete `routeTree.gen.ts` first to force regeneration
- Settings pages go in `apps/web/src/routes/_auth/settings/` — add nav items in the parent `settings.tsx` layout
- oRPC client queries use `useQuery(orpc.<router>.<procedure>.queryOptions())` and mutations use `useMutation` with `orpc.<router>.<procedure>.call(input)` + `queryClient.invalidateQueries`
- Dialog component uses Base UI (`@base-ui/react/dialog`), not Radix — `DialogTrigger` uses `render` prop for custom trigger elements
- WebSocket: use `@hono/node-ws` with `createNodeWebSocket({ app })` → get `upgradeWebSocket` and `injectWebSocket`; must call `injectWebSocket(server)` after `serve()`
- WebSocket types: use `WSContext` from `hono/ws` (not `ServerWebSocket`) for the ws parameter in handlers
- WebSocket route must be registered before CORS middleware to avoid upgrade issues
- `packages/shared/` follows same pattern as `packages/db/` — exports via `./src/index.ts`, uses `@myagents/config/tsconfig.base.json`
- Zod v4 `z.record()` requires two arguments: `z.record(z.string(), z.unknown())` (not single-arg like v3)
---

## 2026-03-18 - US-001
- Added Node model (id, name, userId, lastSeen, status enum) and Agent model (id, slug, name, description, type enum, nodeId, userId, status enum, adapterConfig, shared) in `packages/db/prisma/schema/agents.prisma`
- Added `nodes` and `agents` relations to User model in `auth.prisma`
- Enums: NodeStatus (online/offline), AgentStatus (online/offline), AgentType (hermes/openclaw/custom)
- `@@unique([userId, slug])` ensures slugs are unique per user
- Indexes on Node.userId, Agent.nodeId, Agent.slug, Agent.userId
- Files changed: `packages/db/prisma/schema/agents.prisma` (new), `packages/db/prisma/schema/auth.prisma` (modified)
- **Learnings for future iterations:**
  - Schema validates via `prisma generate` — no DB connection needed
  - Slug validation ([a-zA-Z0-9-]+) will need application-level enforcement (Prisma doesn't support regex constraints)
  - The `@@unique` on compound fields automatically creates an index, so the separate `@@index([userId])` on Agent provides an additional single-column index for queries filtering by userId alone
---

## 2026-03-18 - US-002
- Added Conversation model (id, title, createdAt, updatedAt, userId, agentId) and Message model (id, conversationId, senderType enum, senderId, content, createdAt) in `packages/db/prisma/schema/conversations.prisma`
- Added SenderType enum (user/agent)
- Added `conversations` relation to User model in `auth.prisma` and Agent model in `agents.prisma`
- Indexes on Conversation.userId, Conversation.agentId, Message.conversationId
- Files changed: `packages/db/prisma/schema/conversations.prisma` (new), `packages/db/prisma/schema/auth.prisma` (modified), `packages/db/prisma/schema/agents.prisma` (modified)
- **Learnings for future iterations:**
  - When adding a new model with relations, remember to add the reverse relation field on the related models (e.g., `conversations Conversation[]` on both User and Agent)
  - Each domain schema file can reference models from other schema files — Prisma's multi-file schema handles cross-file references automatically
---

## 2026-03-18 - US-003
- Added AgentPermission model (id, agentId, targetAgentId, createdBy, createdAt) with unique constraint on (agentId, targetAgentId) in `packages/db/prisma/schema/permissions.prisma`
- Added ApiKey model (id, keyHash, name, userId, createdAt, lastUsedAt) in same file
- Added `username` (optional, unique) field to User model in `auth.prisma`
- Added `agentPermissions` and `apiKeys` reverse relations to User model
- Added `permissionsGranted` and `permissionsReceived` named relations to Agent model
- Files changed: `packages/db/prisma/schema/permissions.prisma` (new), `packages/db/prisma/schema/auth.prisma` (modified), `packages/db/prisma/schema/agents.prisma` (modified)
- **Learnings for future iterations:**
  - When a model has two relations to the same target model (e.g., AgentPermission → Agent for both agentId and targetAgentId), you must use named relations (`@relation("name")`) on both sides
  - Username field added as optional (`String?`) since existing users won't have one — will need application-level enforcement at signup
  - The `@@unique` on compound (agentId, targetAgentId) prevents duplicate permission grants
---

## 2026-03-18 - US-004
- Added API key oRPC routes: create, list, revoke in `packages/api/src/routers/apiKeys.ts`
- Registered `apiKeysRouter` in `packages/api/src/routers/index.ts`
- Create generates `myagents_` prefixed key with 32 random bytes, stores SHA-256 hash, returns plaintext once
- List returns keys without hashes, ordered by creation date desc
- Revoke checks ownership before deleting
- All routes use `protectedProcedure` for authentication
- Files changed: `packages/api/src/routers/apiKeys.ts` (new), `packages/api/src/routers/index.ts` (modified)
- **Learnings for future iterations:**
  - oRPC routers are plain objects with procedure properties — add to `appRouter` in `routers/index.ts` to register
  - Use `protectedProcedure` for user-scoped routes, `adminProcedure` for admin-only
  - Access user ID via `context.session.user.id`
  - Prisma model name is lowercase camelCase of the schema model (e.g., `prisma.apiKey`)
  - No bcrypt in project — use Node.js `crypto` SHA-256 for API key hashing (sufficient for high-entropy keys)
  - Pre-existing `web-push` type error in server doesn't block API package typecheck
---

## 2026-03-18 - US-005
- Added API key management UI page at `/_auth/settings/api-keys`
- Create key form with name input, displays plaintext key once with copy button and warning
- List of existing keys showing name, creation date, last used date
- Revoke button with confirmation dialog on each key
- Added "API Keys" nav item with KeyRound icon to settings layout sidebar
- Files changed: `apps/web/src/routes/_auth/settings/api-keys.tsx` (new), `apps/web/src/routes/_auth/settings.tsx` (modified), `apps/web/src/routeTree.gen.ts` (regenerated)
- **Learnings for future iterations:**
  - TanStack Router route tree must be regenerated when adding new route files — delete `routeTree.gen.ts` and run `pnpm vite build` from `apps/web/` to trigger the vite plugin
  - `@tanstack/router-cli generate` alone doesn't regenerate the route tree reliably in this project
  - Dialog uses Base UI's `@base-ui/react/dialog` — `DialogTrigger` takes a `render` prop (e.g., `render={<Button />}`) rather than Radix's `asChild`
  - `DialogClose` also uses `render` prop pattern for custom close elements
  - oRPC client pattern: `useQuery(orpc.apiKeys.list.queryOptions())` for queries, `orpc.apiKeys.create.call(input)` inside `useMutation.mutationFn` for mutations
  - Invalidate queries after mutation: `queryClient.invalidateQueries({ queryKey: orpc.apiKeys.list.queryOptions().queryKey })`
  - Referenced solved problem `ui/shadcn/dashboard-conventions` for overlay patterns (Dialog for confirmation)
---

## 2026-03-18 - US-006
- Added WebSocket upgrade handler at `/ws` route on the Hono server using `@hono/node-ws`
- Created connection registry (`connectionRegistry`) mapping nodeId → WebSocket and userId → WebSocket[]
- Two connection types: client (web app, session cookie auth) and node (CLI, API key auth)
- Authentication: clients use session cookies via Better Auth, nodes use API key hash verification
- Node connections auto-create or update Node records in DB; disconnection marks node + agents offline
- Files changed: `apps/server/src/ws.ts` (new), `apps/server/src/index.ts` (modified), `apps/server/package.json` (modified)
- **Learnings for future iterations:**
  - `@hono/node-ws` exports `createNodeWebSocket` which returns `{ upgradeWebSocket, injectWebSocket, wss }`
  - The WebSocket type for handler params is `WSContext` from `hono/ws`, NOT `ServerWebSocket` from `@hono/node-ws`
  - `injectWebSocket(server)` must be called after `serve()` returns the HTTP server
  - WebSocket route should be registered before CORS `/*` middleware to ensure the upgrade request isn't intercepted
  - API key authentication reuses the SHA-256 hash pattern from `apiKeys.ts` — the `hashKey` function should be shared in a future refactor
  - Used kctx `@hono/node-ws` documentation for the WebSocket setup pattern
---

## 2026-03-18 - US-007
- Created `packages/shared/` package with Frame protocol types, Zod schemas, and helpers
- Defined Frame type: `{ type: 'req'|'res'|'event', id: string, method: string, payload: unknown, error?: string }`
- Added all frame methods: auth, message.send, message.response, message.chunk, message.done, agent.register, agent.heartbeat, agent.status
- Added Zod schemas for each method's payload with `parseFrame()`, `createFrame()`, `serializeFrame()` helpers
- Updated `ws.ts` to parse incoming frames and dispatch to `handleFrame()`, respond with frame protocol
- Implemented heartbeat: server pings nodes every 30s, marks offline after 90s of silence via `startHeartbeat()`
- Added `lastSeen` tracking on NodeConnection, updated on any message
- Wired `startHeartbeat()` into `index.ts` after server starts
- Files changed: `packages/shared/` (new package), `apps/server/src/ws.ts` (modified), `apps/server/src/index.ts` (modified), `apps/server/package.json` (modified)
- **Learnings for future iterations:**
  - `packages/shared/` follows same structure as other internal packages — `@myagents/shared` with `workspace:*`
  - Zod v4 requires `z.record(z.string(), z.unknown())` — single-arg `z.record(z.unknown())` is a TS error
  - WSContext `.readyState === 1` checks for OPEN state before sending
  - The `handleFrame` switch covers all FrameMethod variants — future stories (US-010, US-012) will add actual routing logic
  - Heartbeat uses `setInterval` — the returned handle can be used for cleanup in tests
---
